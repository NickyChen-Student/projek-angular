import { AmountColor } from './amount-color';

describe('AmountColor', () => {
  it('should create an instance', () => {
    const directive = new AmountColor();
    expect(directive).toBeTruthy();
  });
});
